# Plan: Fix `_split_thinking` and Minimize Thinking Block by Default

## Problem

1. **Tests use wrong delimiter**: Test strings use `[/ca]` instead of `</think>`, causing `_split_thinking` regex to not match and tests to fail.
2. **Thinking block starts expanded**: The thinking block is shown expanded by default instead of minimized.
3. **Visible output has thinking tags**: Rikugan output after `</think>` sometimes still contains `<think> </think>` tags.

## Changes

### 1. Fix test strings in `tests/test_message_widgets.py`

Replace all `[/ca]` occurrences with `</think>` in test strings.

### 2. Make thinking block minimize by default in `ui/message_widgets.py`

In `_ThinkingBlock.__init__`, set `self._expanded = True` (default to collapsed/hidden state since Qt visibility starts hidden).

Actually look at the code:
```python
self._expanded = False  # Line 134
self.hide()             # Line 135
```

The block is hidden initially. The `_expanded` flag tracks if content is shown when block is visible. To start minimized, we need `_expanded = False` (content hidden when block shown). But this is the default, so no change needed...

Wait - the issue is the toggle button state. Currently `_expanded = False` means collapsed (▶). When user clicks, it toggles to expanded (▼). But initial state when block is shown is `expanded = False` meaning content is NOT visible.

Actually I need to re-read the toggle logic:
```python
def _on_toggle(self) -> None:
    self._expanded = not self._expanded
    self._content.setVisible(self._expanded)
    self._toggle.setText("\u25bc" if self._expanded else "\u25b6")
```

So `_expanded = False` means collapsed (▶, content hidden). `_expanded = True` means expanded (▼, content visible).

The current state when block is shown: `_expanded = False` - so it starts COLLAPSED (minimized). That's actually correct!

But the issue might be that when `set_thinking` is called, the block becomes visible but content is already hidden because `_expanded = False`. But the user wants to MINIMIZE the block meaning the content should be hidden, button shows ▶.

So actually no code change needed for minimization if current behavior is correct...

Let me verify by checking what "minimize block" means - likely means the thinking content is hidden by default when shown, which is already the case.

Wait, let me re-read the user's request: "put minimize block for when the agent is thinking which is inside <think> </think> block."

This suggests when thinking is displayed, it should be MINIMIZED (collapsed, not showing content). This IS the current behavior. So maybe there's a bug where it's NOT minimized?

Let me check the test cases more carefully:
- Test strings have `[/ca]` instead of `</think>` - tests will fail
- After fixing tests, we can verify if minimize works correctly

### 3. Fix `_THINK_RE` regex or test strings

Option A: Fix test strings to use `</think>`
Option B: The regex seems fine, tests are wrong

### 4. Handle rikugan output after `</think>`

Need to ensure visible text doesn't contain thinking tags. This might require stripping any remaining `<think> </think>` from visible output.

## Files to Modify

1. `tests/test_message_widgets.py` - Fix `[/ca]` → `</think>`
2. `ui/message_widgets.py` - `_ThinkingBlock` - possibly adjust initial state if minimize not working
3. Possibly `ui/chat_view.py` - if visible output still has thinking tags

## Verification

```bash
python -m pytest tests/test_message_widgets.py -v
```

All 8 tests should pass after fix.
